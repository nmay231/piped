from collections import defaultdict

first_pass = defaultdict(list)


def find_entries_in_module(ast_ctx, meta_data):
    print("found entry!")


first_pass["receiveEntry"].append(find_entries_in_module)
